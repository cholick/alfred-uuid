### Alfred UUID Workflow
Alfred workflow to generate a random v4 UUID and put it on the clipboard.

It just invokes the cli command `uuidgen`, but there have been a few times building out test data
coding when it's been nice to not have to hop over to the terminal and type `uuidgen | pbcopy`.
